import { toast } from "react-toastify"


export const handleSuccess = (msg) => {
  toast.success(msg, {
    position: 'positon-right'
  })
}


export const handleError = (msg) => {
  toast.success(msg, {
    position: 'positon-right'
  })
}

